# 为 MengDie Code 做贡献

感谢你愿意帮助 MengDie Code。项目目前处于早期阶段，我们更看重清晰的问题、可复现的场景和小而完整的改进，而不是一次加入大量尚未验证的功能。

## 语言

- 项目的首选维护语言是中文。
- 英文 Issue、讨论和 Pull Request 同样欢迎，不要求贡献者会中文。
- 代码标识符、Go 注释和公开协议字段优先使用清晰英文；面向用户的文案默认中文优先。
- 修改 README 中的重要产品信息时，请尽量同步 `README_EN.md`；如果无法翻译，可以在 PR 中注明，由维护者协助。

## 开始之前

1. 阅读 [README.md](./README.md) 和 [ARCHITECTURE.md](./ARCHITECTURE.md)。
2. 搜索现有 Issue 和讨论，避免重复工作。
3. 较大的行为变化先开 Issue，说明用户痛点、最小方案、风险和验收方式。
4. 安全漏洞不要创建公开 Issue，参见 [SECURITY.md](./SECURITY.md)。

## 当前贡献重点

- 真实 Coding Agent 痛点与可复现任务；
- 架构设计审查；
- Coding、长任务、记忆可信度评测；
- 国内模型 Provider 的兼容性样例；
- Windows 文件、进程和权限边界；
- 小型、可测试、属于当前里程碑的实现；
- 中文文档质量和英文入口。

daemon、Web、异步 Swarm、向量记忆和记忆图谱暂不属于 v0.1。除非有明确数据证明必要，请不要提前实现这些模块。

## 本地开发

要求：

- Go 1.26+
- Git

常用检查：

```bash
go fmt ./...
go vet ./...
go test ./...
```

提交前请确保：

- `go test ./...` 通过；
- `go vet ./...` 通过；
- `gofmt -l .` 没有输出；
- 新行为有测试或明确说明暂时无法自动测试的原因；
- 文档没有把规划中的能力写成已经完成。

## Pull Request

一个 PR 尽量只解决一个问题。描述中请包含：

- 改了什么；
- 为什么要改；
- 对用户或开发者有什么影响；
- 如何验证；
- 是否涉及安全、兼容性、记忆 schema 或事件协议变化。

事件、存储和公开命令一旦被用户依赖就很难修改。涉及这些边界的 PR 应同时说明迁移与向后兼容策略。

## 提交信息

提交信息使用简短祈使句即可，中文或英文均可，例如：

```text
完善记忆来源校验
add event store contract tests
```

## 行为准则

参与项目即表示同意遵守 [行为准则](./CODE_OF_CONDUCT.md)。讨论技术判断时可以直接，但请针对方案和证据，不针对个人。

