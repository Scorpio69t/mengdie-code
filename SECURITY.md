# 安全策略

MengDie Code 会读取和修改代码、执行命令并连接模型服务，因此安全问题属于项目的核心质量问题。

## 当前支持状态

项目尚未发布稳定版本。当前 `main` 分支仅用于早期开发，不应部署到生产环境，也不应在包含敏感凭据或不可恢复数据的环境中运行。

## 报告漏洞

请不要通过公开 Issue 披露尚未修复的漏洞。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口：

https://github.com/Scorpio69t/mengdie-code/security/advisories/new

报告中请尽量包含：

- 受影响的提交或版本；
- 复现步骤或最小示例；
- 预期影响和攻击前提；
- 已知的缓解措施；
- 是否已经向其他人披露。

维护者会确认收到报告、评估影响，并在修复发布后公开致谢；如报告者希望匿名，请明确说明。

## 重点关注范围

- 路径穿越和工作目录逃逸；
- shell 参数注入、权限提升和审批绕过；
- 环境变量、密钥和敏感文件泄漏；
- 网络访问与 MCP 工具带来的提示注入；
- 事件重放导致副作用重复执行；
- 恶意或错误记忆跨会话传播；
- Patch Journal 或 rewind 覆盖用户后续修改；
- Provider 响应、日志和 Artifact 中的敏感信息泄漏。

